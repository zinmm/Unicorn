<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zin.unicorn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.zin.unicorn.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/UnicornTheme">

        <activity
            android:name="com.zin.unicorn.module.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/StartAppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/AppTheme"
            android:name="com.zin.unicorn.module.HomePageActivity" />

        <activity
            android:theme="@style/AppTheme"
            android:name="com.zin.unicorn.module.user.activity.ProfileActivity" />

    </application>

</manifest>