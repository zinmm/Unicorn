project.ext {
    applicationId = "com.zin.unicorn"

    compileSdkVersion = 28

    buildToolsVersion = "28.0.3"

    minSdkVersion = 15
    targetSdkVersion = 26

    versionCode = 1
    versionName = "1.0"
}